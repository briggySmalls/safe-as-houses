{
  elasticSettings {
    url = "http://localhost:9200"
    url = ${?SCRAPEMOVE_ES_URL}
    index = "house-index-2"
    index = ${?SCRAPEMOVE_ES_INDEX}
  }

  serverSettings {
    host = "localhost"
    host = ${?SCRAPEMOVE_SERVER_HOST}
    port = "8080"
    port = ${?SCRAPEMOVE_SERVER_PORT}
  }
}